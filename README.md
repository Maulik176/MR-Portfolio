# Maulik Portfolio (Reconstructed)

This project was reconstructed from production source maps:

- `/static/js/main.2e8d6cf1.js.map`
- `/static/css/main.145a1e5f.css.map`

## Run locally

```bash
npm install
npm start
```

## Notes

- The `src/` code and `scss` files were extracted from source maps.
- Asset files in `src/assets` were recovered from bundle references (hashed media files and inline base64 assets).
- Content sections query Sanity, so set env vars in `.env` (see `.env.example`).
